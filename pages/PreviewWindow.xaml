<ui:FluentWindow x:Class="superClipboard.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:superClipboard"
        mc:Ignorable="d"
        Title="预览" 
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        MinHeight="400" MinWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar x:Name="TitleBar" 
             Title="超级剪贴板" 
             CloseWindowByDoubleClickOnIcon="True"
             Grid.Row="0">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="/Resources/icon.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <!-- 标题栏 -->
        <Border Grid.Row="1" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TitleText" FontSize="16" FontWeight="Bold"/>
                <TextBlock Grid.Column="1" x:Name="TimestampText"/>
            </Grid>
        </Border>
        
        <!-- 内容区域 -->
        <ScrollViewer Grid.Row="2" Padding="10">
            <Grid>
                <!-- 文本内容 -->
                <TextBox x:Name="TextContentBox" 
                       TextWrapping="Wrap" 
                       VerticalScrollBarVisibility="Auto"
                       IsReadOnly="True"
                       FontFamily="Consolas"
                       FontSize="12"
                       Visibility="Collapsed"/>
                
                <!-- 图片内容 -->
                <Image x:Name="ImageContentBox" 
                       Stretch="Uniform"
                       MaxHeight="500"
                       Visibility="Collapsed"/>
                
                <!-- 文件列表 -->
                <ui:ListView x:Name="FileListBox" 
                        Visibility="Collapsed"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ui:ListView.View>
                        <ui:GridView>
                            <GridViewColumn Header="文件名" DisplayMemberBinding="{Binding Name}" Width="200"/>
                            <GridViewColumn Header="大小" DisplayMemberBinding="{Binding SizeFormatted}" Width="100"/>
                            <GridViewColumn Header="路径" DisplayMemberBinding="{Binding FullPath}" Width="300"/>
                        </ui:GridView>
                    </ui:ListView.View>
                </ui:ListView>
                
                <!-- 未知类型 -->
                <TextBlock x:Name="UnknownContentText" 
                         TextWrapping="Wrap"
                         Visibility="Collapsed"/>
            </Grid>
        </ScrollViewer>
        
        <!-- 底部按钮 -->
        <Border Grid.Row="3" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CopyButton" Content="复制到剪贴板" Margin="5" Click="CopyButton_Click" Width="120" Height="30"/>
                <Button x:Name="CloseButton" Content="关闭" Margin="5" Click="CloseButton_Click" Width="80" Height="30"/>
            </StackPanel>
        </Border>
    </Grid>
</ui:FluentWindow>