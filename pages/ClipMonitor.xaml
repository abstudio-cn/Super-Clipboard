<UserControl x:Class="superClipboard.ClipMonitor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:superClipboard"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Unloaded="UserControl_Unloaded"
             >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="快捷键：" VerticalAlignment="Center" Margin="20,0,0,0" Foreground="White"/>
            <TextBlock Text="Ctrl+V - 普通粘贴" VerticalAlignment="Center" Margin="10,0" Foreground="White"/>
            <TextBlock Text="Ctrl+Alt+V - 模拟键盘输入" VerticalAlignment="Center" Margin="10,0" Foreground="White"/>
        </StackPanel>

        <!-- 内容区域 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 信息显示 -->
            <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="1" 
                    CornerRadius="5" Margin="0,0,0,10" Padding="10">
                <StackPanel>
                    <TextBlock Text="当前剪贴板内容：" FontWeight="Bold" Margin="0,0,0,5" Foreground="White"/>
                    <StackPanel x:Name="InfoPanel">
                        <TextBlock x:Name="DataTypeText" Text="数据类型：无" Foreground="White"/>
                        <TextBlock x:Name="TimeText" Text="时间：无" Foreground="White"/>
                        <TextBlock x:Name="PreviewText" Text="预览：无" TextWrapping="Wrap" Foreground="White"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 内容预览 -->
            <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" 
                    CornerRadius="5" Padding="10">
                <ScrollViewer Foreground="White">
                    <Grid>
                        <!-- 文本内容 -->
                        <TextBox x:Name="TextContentBox" 
                               TextWrapping="Wrap" 
                               VerticalScrollBarVisibility="Auto"
                               IsReadOnly="True"
                               Visibility="Collapsed"/>

                        <!-- 图片内容 -->
                        <Image x:Name="ImageContentBox" 
                               Stretch="Uniform"
                               Visibility="Collapsed"/>

                        <!-- 文件列表 -->
                        <ListBox x:Name="FileListBox" 
                               Visibility="Collapsed"/>

                        <!-- 无内容提示 -->
                        <TextBlock x:Name="NoContentText" 
                                 Text="剪贴板为空或内容不可预览" 
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"
                                 Foreground="White"
                                 FontSize="16"/>
                    </Grid>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
